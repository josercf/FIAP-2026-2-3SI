# Dependências do serviço de frete da LogiTech (Aula 10).
# Versões com piso mínimo: o devcontainer instala a mais recente da linha.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27

# A suíte de testes da aula. `pytest-mock` não entra de propósito: o dublê
# vem do `unittest.mock`, que é biblioteca padrão e existe em qualquer
# projeto Python, sem pedir dependência nova ao time de plataforma.
pytest>=8.3
