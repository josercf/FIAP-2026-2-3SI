[pytest]
# Rodar sempre da raiz do laboratório: `python3 -m pytest`
# `pythonpath` põe o verificar.py no caminho de importação sem exigir
# instalação nem PYTHONPATH exportado à mão.
pythonpath = .
testpaths = tests
addopts = -q
